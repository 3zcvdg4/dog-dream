# DEVELOPMENT_PLAN.md

## 项目定位

`dogdream` 是一个以“狗狗做梦”为叙事入口的个人作品网站。

它不是传统简历页，而是把个人介绍、作品浏览、项目详情和情绪氛围串成一条完整体验链路：

1. 用户先在首页看到狗狗入睡。
2. 通过梦泡进入梦境走廊。
3. 在走廊中聚焦作品，再进入三级项目详情。
4. 通过“醒来”回到首页，重新回到狗狗与梦境循环。

当前前端技术栈以 `Vite + React + Three.js` 为核心，3D 部分主要依赖 `@react-three/fiber`、`@react-three/drei` 和 `@react-three/rapier`。

## 当前体验链路

### 1. 首页加载

- 首次进入 `/` 时先显示 loading 页面，而不是直接进入首页内容。
- loading 页面包含百分比数字、进度条和 `Loading...` 文案。
- 预加载内容包括：首页狗狗视频、坐垫、梦泡、关于挂绳卡片贴图等首页关键资源。

### 2. 首页 intro 阶段

- loading 完成后进入首页主场景。
- 首页顶部是标题 `EVERYTHING YOU SEE IS PART OF A DREAM`。
- 场景主体由狗狗视频、坐垫、梦泡、背景构成。
- 首次进入首页时，导航栏会自动打开“关于”面板。
- 这里的“关于”不是普通弹窗，而是一个带物理摆动和拉走关闭动画的 3D 挂绳卡片系统。
- 在 intro 阶段，如果关于卡片还没关闭，首页视频播放会先被阻塞；关闭后才进入正常首页时序。

### 3. 首页梦境入口

- 狗狗首页流程的核心状态包括：`intro`、`waking`、`sleepingAgain`、`dreamReady`、`awake`。
- 当流程进入 `dreamReady` 后，梦泡才真正可见且可点击。
- 当前梦泡不是静态入口，而是包含出现态、idle 扩散态和破碎态。
- idle 状态下，梦泡外侧会持续出现双圈扩散动画。
- 从首页进入走廊时，点击的是梦泡本体，扩散起点使用梦泡内部的固定锚点。

### 4. 首页进入走廊

- 点击梦泡后，会先执行首页到走廊的 ripple 转场，而不是硬切页。
- 当前转场结构是：首页快照层 + Canvas 波纹位移 + 白层覆盖 + 白层退场。
- 这个 ripple 是当前桌面链路里的固定体验，不能随意改回纯白过渡。

### 5. 梦境走廊

- 进入 `/corridor` 后，用户处于可滚动的 3D 梦境走廊。
- 走廊是封闭的白灰空间，两侧排列画框，远端有出口光感。
- 用户通过滚轮或触摸手势沿走廊纵深漫游。
- 点击某个画框后，不会立刻进入详情，而是先进入聚焦态。
- 聚焦态下镜头对准当前作品，并显示右侧项目说明卡片。
- 右侧卡片当前提供两个动作：`进入深梦` 与 `返回走廊`。
- 聚焦海报本身也可以直接进入三级项目详情，不再只依赖按钮。

### 6. 三级项目详情页

- 进入 `/project/:slug` 后，用户离开走廊聚焦态，进入独立的项目详情页。
- 详情页当前分为 editorial 与常规项目两类布局。
- 页面外部操作文案已统一改成中文，当前使用的是 `回到走廊` 与 `醒来`。
- 代码内部仍保留 `wakeUp()` 作为逻辑函数名，但界面不再对外显示英文 `wake up`。

### 7. 醒来与再次入睡

- 从走廊或详情页点击 `醒来` 后，返回首页并进入唤醒链路。
- 唤醒时会触发梦泡破碎，并播放醒来视频。
- 醒来视频结束后，首页出现 `睡觉` 按钮。
- 点击 `睡觉` 后播放再次入睡视频，随后梦泡重新出现，回到可再次入梦的循环。

## 当前站点结构

### 路由与页面切换

- 当前不是 `React Router` 路由方案。
- 站点由 `src/App.jsx` 手动维护路径与视图状态。
- 当前主要路径包括：
  - `/`：首页
  - `/corridor`：梦境走廊
  - `/project/:slug`：三级项目详情页
  - `/steam-lab`：预留实验入口

### 导航栏

- 全站导航统一由 `SiteNav` 承担，不同页面使用不同 variant。
- 首页导航：`首页`、`选择深梦`、`关于`。
- 走廊导航：继续使用统一导航体系，不再单独各写一套顶栏。
- 详情页导航：同样走 `SiteNav variant="detail"`，并提供回到走廊/返回首页相关动作。
- “关于”内容已经整合在导航挂出的关于小卡片/挂绳体系里，后续不要再把 `about / now / resume` 当作待补功能。

## 当前已实现状态

### 首页

- 已实现 loading 首屏与预加载进度反馈。
- 已实现首页标题、狗狗视频、坐垫、梦泡、睡觉按钮的完整时序。
- 已实现首页关于挂绳卡片自动打开、摆动、拖拽、拉走关闭等动画。
- 已实现首页梦泡出现、idle 双圈扩散、破碎、进入走廊的完整链路。
- 首页当前主要视频源仍是：`walk-ffmpeg-1.webm`、`wakeup-ffmpeg-1.webm`、`sleep-ffmpeg-1.webm`。

### 走廊

- 已实现走廊漫游、聚焦、聚焦卡片、进入深梦、返回走廊、醒来。
- 已实现走廊中的灯光、烟雾、表面波纹等氛围层。
- 已实现聚焦态右侧说明卡片的新背景与新排版。
- 已实现聚焦海报直接进入详情页。
- 当前“狗脚印”不再是站点真实体验链路中的可见组成部分；源码中仍残留部分旧常量、旧状态和旧样式，需要视为历史残留而不是当前功能说明。

### 详情页

- 已实现 Dogdream / ORTUR 等项目的三级详情结构。
- 已实现 editorial 与常规项目的两类详情布局。
- 详情页当前对外按钮文案已统一为中文，不再沿用旧文档里的英文按钮描述。

### 响应式

- 已实现首页与详情页的大量响应式规则。
- 已实现竖屏场景下的横屏提示遮罩。
- 当前响应式不是单一流式方案，而是建立在大量定向断点上的连续修正体系。
- 因此后续判断页面表现时，应以 `src/styles/global.css` 的现行规则为准，不能再沿用旧文档里那套早期尺寸说明。

## 当前主要文件

- `src/App.jsx`：全站入口、手写路由、loading、首页进入模式、转场、横屏提示。
- `src/pages/Home.jsx`：首页视频时序、梦泡显隐/破碎、睡觉按钮、进入走廊锚点。
- `src/components/SiteNav.jsx`：全站导航与关于面板宿主。
- `src/components/AboutLanyard.jsx`：关于挂绳卡片、物理摆动、关闭动画。
- `src/components/DreamTransitionOverlay.jsx`：首页进入走廊的 ripple 转场。
- `src/pages/DreamCorridor.jsx`：走廊漫游、聚焦态、聚焦卡片、灯光、烟雾、表面波纹。
- `src/components/FramePortal.jsx`：走廊画框/海报展示与交互入口。
- `src/pages/ProjectDream.jsx`：三级项目详情页。
- `src/styles/global.css`：全站样式、首页时序样式、导航样式、聚焦卡片样式、响应式断点。
- `src/data/projects.js`：走廊项目列表与基础项目信息。
- `src/data/siteProfile.js`：关于信息与联系信息数据源。

## 内容与素材工作路径

- 三级页内容主工作目录：`content/`
- 中文策划与项目资料归档：`dog dream project/`
- 静态资源主目录：`public/assets/`
- 海报与项目相关资源：`public/projects/`、`poster/`

当前原则：运行时会直接用到的正式素材，应优先收敛到 `public/` 下的稳定路径，不要把 `dist/` 当长期素材目录。

## 当前需要继续收口的方向

### 1. 响应式继续收口

- 虽然现有断点已经很多，但用户已明确指出“自适应很多地方不对”。
- 后续应继续以真实设备尺寸和真实视觉结果为准，逐段修正首页、聚焦态卡片和详情页排版。

### 2. 文档与源码历史残留清理

- 旧版开发文档里仍混有已经失效的描述，例如英文按钮、脚印、过时流程等。
- 源码中也保留了部分未彻底清除的旧状态或旧样式，后续应在不破坏当前体验的前提下逐步清理。

### 3. 联系方式与内容真实性补完

- `siteProfile` 中仍有占位式联系信息。
- 在正式上线前，需要替换为真实联系方式，并统一检查 about 内容与作品内容是否已经全部定稿。

### 4. 部署与上线动作独立推进

- 部署、域名、国内访问、联系/留言能力的实现链路，不再写在本文件里混合维护。
- 这部分以后以根目录 `DEPLOYMENT_GUIDE.md` 为唯一执行文档。

## 历史备份说明

- 历史快照、回退补丁和阶段性备份统一保留在 `backups/`。
- `DEVELOPMENT_PLAN.md` 今后应只描述“当前真实状态 + 下一阶段重点”，不再继续堆积大段失效历史过程。
