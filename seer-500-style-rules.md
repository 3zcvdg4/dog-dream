# Seer 500 Style Rules

这份文档只整理 `seer-500` 当前已经成立、并且应该继续沿用的页面规则。  
目标不是发明新风格，而是把前面 4 个已经做好的板块的共性抽出来，作为 `Closing` 的约束。

适用范围：

- `src/components/project/ProjectSeerCaseStudy.jsx`
- `src/data/projectContents/project-04.js`
- `src/styles/global.css`

## 1. 页面节奏

`seer-500` 不是自由拼贴页，而是有明确的黑白节奏：

1. `Hero / Overview reveal`：深色
2. `Touchpoints`：浅色
3. `Campaign`：深色
4. `Packaging System`：浅色
5. `Closing`：应继续遵守黑白交替，优先使用深色 section 承接

规则：

- section 之间要靠**整块底色切换**建立节奏，而不是靠单个卡片颜色切换
- 不能在浅色 section 后面再随意接一个浅色 section，打断整体节拍

## 2. 颜色系统

当前已成立的主色关系：

- 品牌强调色：`var(--seer-brand)`，即青绿色
- 深色 section 背景：`#0b0e13`
- 浅色 section 背景：`#eef1f4`
- 深色正文：`rgb(213 226 242 / 0.84)` 及相邻层级
- 浅色正文：`rgb(22 31 41 / 0.84)` 及相邻层级

规则：

- 青绿色只作为**强调色**使用：label、active、重点块、少量线索
- 不要把青绿色大面积铺满所有组件
- 白底 section 内部的边框必须非常轻：`rgb(17 25 35 / 0.08~0.12)`
- 深色 section 内部边框使用低透明度青色或白色：`rgb(127 224 255 / 0.1~0.16)`

## 3. 字体层级

### 英文标题 / 标签

- 字体：`Inter`
- 用途：大标题、section label、tab、导航、关键词

现有规则参考：

- section label：11–12px，600，uppercase，较大 letter-spacing
- 大标题：`clamp(34px, 4.6vw, 62px)` 到 hero 的 `clamp(72px, 11vw, 168px)`

### 中文正文

- 字体：`Arial`, `Helvetica Neue`, `Noto Sans SC`
- 用途：lead、detail、说明文字

现有规则参考：

- lead：更大、更靠前，行高约 `1.45`
- detail / body：`16–19px` 左右，行高 `1.65–1.75`

规则：

- 英文系统信息和展示型标题用 `Inter`
- 中文叙述性内容不用 `Inter`，继续沿用现有正文体系
- 不要在 `Closing` 里混用一套新的文字气质

## 4. 布局规则

### 内容宽度

- 主内容容器统一使用大宽度：`width: min(1500px, 90%)`
- 每个 section 都是完整横向展开，不是居中小卡片页面

### 模块排版

- section 标题先出现，再进入视觉或交互主体
- 图片、设备、海报、包装卡的布局都是**宽屏展示优先**
- 不要突然切成“一个独立小白卡浮在中间”的展示方式

规则：

- `Closing` 必须仍然看起来像这个长页面里的一个 section
- 不能像单独弹窗、专题海报、临时 dashboard

## 5. 圆角和边框

前面已经成立的圆角规则不是统一一个值，而是按模块层级分配：

- Overview 主视觉：`14px`
- Campaign 海报：`12px`
- Packaging 外层 card：`24px`
- Touchpoints pills：外层 `16px`，按钮 / indicator `12px`

结论：

- **外层大容器圆角更大**
- **内部图片块圆角更小**
- **不是所有东西都同一个圆角**

规则：

- `Closing` 如果有外层信息板，外层圆角应大于内部图块圆角
- 内部缩略图 / 拼贴块圆角建议落在 `8px ~ 12px`
- 不能出现边框、圆角、卡片层级彼此无关的情况

## 6. 图片使用规则

前面好的部分里，图片处理有明确规律：

- Overview：单张主视觉，宽图，轻圆角
- Campaign：高图连续展开，重在节奏
- Packaging：大视觉区 + 右侧信息区，图片本身尽量完整展示

规则：

- `Closing` 里的图片不能只是为了“填满”
- 结束区如果使用拼贴，必须来自前文已出现过的核心内容：
  - 渠道触点
  - 品牌传播
  - 包装体系
- 拼贴图应作为“前文回收”，而不是新内容

## 7. 组件语气

前面已经成立的组件语气是：

- 冷静、干净、编辑感
- 信息感强，但不是科技产品后台
- 有展示性，但不做多余装饰

规则：

- 不要出现像按钮一样重的标签
- 不要做多余玻璃拟态、漂浮 hover 卡、过度阴影
- 不要给 `Closing` 加和前文无关的视觉语言

## 8. Closing 专属约束

`Closing` 的职责只有两个：

1. 回收前文已经讲过的内容
2. 用一个完整版面结束这页

因此：

- 应该使用前文已有元素重新组织
- 应该保持这个页面的字体、边框、圆角、色彩层级
- 应该是“收束板”，不是“新风格实验区”

### Closing 不该做什么

- 不该像临时拼出来的小白卡
- 不该引入新的按钮系统
- 不该打断黑白 section 节奏
- 不该出现一套独立于前文的边框 / 阴影 / 卡片语言

### Closing 应该怎么做

- 放在深色 section 上，形成浅色包装区后的收尾反差
- 内部可以有一个浅色信息板 / 拼贴板
- 标题使用 `Inter`，正文继续用中文正文字体体系
- 拼贴内容仅取自前文核心图组
- 标签 / 信息轨应该更像说明索引，而不是按钮组

## 9. 实施时的优先级

重做 `Closing` 时，优先级如下：

1. 先守住 section 黑白节奏
2. 再守住字体系统
3. 再统一边框和圆角层级
4. 最后才是拼贴内容和细节修饰

如果这四条冲突，优先保前 3 条。
